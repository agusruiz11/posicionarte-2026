node_modules
.next/

# Env — nunca commitear
.env.local
.env*.local

# Archivos de assets originales (pesados)
src/assets/videos/*.mp4
src/assets/videos/*.mov

# Build de export estático de la etapa Hostinger. Ya no se genera:
# el deploy es Vercel desde GitHub.
out/


Claude outputs/